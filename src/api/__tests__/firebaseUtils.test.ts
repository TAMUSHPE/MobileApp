import { db, auth, storage } from "../../config/firebaseConfig";
import * as firebaseUtils from "../firebaseUtils";


test("Ensure current user data is initialized correctly", () => {
    expect(true);
});
