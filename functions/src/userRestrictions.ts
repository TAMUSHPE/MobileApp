

const addBlacklist = async (uid: string) => {
    
};

const removeBlacklist = async (uid: string) => {
    
};

const addWatchlist = async (uid: string) => {
    
};

const removeWatchlist = async (uid: string) => {
    
};